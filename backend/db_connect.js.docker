const mariadb = require('mariadb');

const pool = mariadb.createPool({
  host: 'c_mariadb',
  port: 3306,
  user: '${LOGIN_DB}', 
  password: '${PASSWORD_DB}', 
  database: 'info411',
  connectionLimit: 10
});

module.exports = {
  pool
};