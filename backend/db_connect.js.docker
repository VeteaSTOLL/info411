const mariadb = require('mariadb');

const pool = mariadb.createPool({
  host: 'c_maria_db',
  port: 3306,
  user: 'vttvcool', 
  password: 'tvvtcool', 
  database: 'info411',
  connectionLimit: 10
});

module.exports = {
  pool
};